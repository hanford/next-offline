{
  "name": "next-offline-test-app",
  "scripts": {
    "dev": "node server.js",
    "clean": "rm -rf .next static/manifest",
    "build": "next build",
    "start": "yarn clean && yarn build && NODE_ENV=production node server.js",
    "test": "yarn clean && jest",
    "export": "yarn build && next export"
  },
  "dependencies": {
    "mkdirp": "^0.5.1",
    "next": "7.0.2",
    "next-compose-plugins": "^2.1.1",
    "next-manifest": "^2.0.0",
    "next-offline": "https://github.com/derekdowling/next-offline#0270acd",
    "react": "16.6.1",
    "react-dom": "16.6.1",
    "rimraf": "^2.6.2"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  }
}
